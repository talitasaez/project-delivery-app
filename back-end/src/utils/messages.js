const displayNameFormat = '"name" length must be at least 8 characters long';
const emailFormat = '"email" must be a valid email';
const passwordFormat = '"password" length must be at least 6 characters long';

module.exports = {
  displayNameFormat,
  emailFormat,
  passwordFormat,
};